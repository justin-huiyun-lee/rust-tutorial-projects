enum MoveDirection {
    UP,
    DOWN,
    LEFT,
    RIGHT,
}

const WIDTH: u32 = 20;
const HEIGHT: u32 = 10;

fn main() {
    let game_active = true;
    let snake_length = 2;
    let player_direction = MoveDirection::UP;
    
}
